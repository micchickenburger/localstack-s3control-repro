{
  "name": "localstack-s3control-repro",
  "version": "1.0.0",
  "description": "Reproducing s3control issue with localstack",
  "main": "index.js",
  "scripts": {
    "start": "esbuild src/index.ts --bundle --sourcemap --platform=node --target=es2020 --outfile=dist/index.js --watch=forever",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Micah Henning <mhenning3@wisc.edu>",
  "license": "MIT",
  "dependencies": {
    "@cdktf/provider-aws": "^19.40.0",
    "cdktf": "^0.20.9",
    "cdktf-cli": "^0.20.9"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
